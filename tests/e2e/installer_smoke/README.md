# Installer Smoke Tests

Placeholder for OS installer smoke tests:
- Linux AppImage/.deb startup + runtime health
- Windows MSI install/repair/uninstall + runtime health
- macOS notarized app launch + runtime health
