Bundled runtime placeholder.

Release builds replace this directory with a target-specific portable runtime before Tauri packaging.
Do not commit host-machine Python wrappers, absolute symlinks, or system `ffmpeg`/`yt-dlp` binaries here.
