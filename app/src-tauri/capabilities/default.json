{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the main Signal Forge window.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
